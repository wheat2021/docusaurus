---
slug: /migration/v2
---

# 概述

本文档将指导您将现有的 Docusaurus 1 站点迁移到 Docusaurus 2。

我们尽力使这个过程尽可能简单，并提供了一个迁移 CLI。

## 主要区别 {#main-differences}

Docusaurus 1 是一个纯粹的文档站点生成器，使用 React 作为服务器端模板引擎，但不在浏览器中加载 React。

Docusaurus 2 从头开始重建，生成一个单页应用程序，在浏览器中充分利用 React 的强大功能。它允许更多的可定制性，但保留了 Docusaurus 1 的精华部分——易于上手、版本化文档和 i18n。

除此之外，Docusaurus 2 是一个**高性能的静态站点生成器**，可以用来极快地创建常见的内容驱动型网站（例如文档、博客、产品着陆页和营销页面等）。

虽然我们的主要焦点仍然是帮助您正确、完善地制作文档，但使用 Docusaurus 2 构建任何类型的网站都是可能的，因为它只是一个 React 应用程序。**Docusaurus 现在可以用来构建任何网站，而不仅仅是文档网站。**

## Docusaurus 1 结构 {#docusaurus-1-structure}

您的 Docusaurus 1 站点应具有以下结构：

```bash
├── docs
└── website
    ├── blog
    ├── core
    │   └── Footer.js
    ├── package.json
    ├── pages
    ├── sidebars.json
    ├── siteConfig.js
    └── static
```

## Docusaurus 2 结构 {#docusaurus-2-structure}

迁移后，您的 Docusaurus 2 站点可能如下所示：

```sh
├── docs
└── website
    ├── blog
    ├── src
    │   ├── components
    │   ├── css
    │   └── pages
    ├── static
    ├── package.json
    ├── sidebars.json
    ├── docusaurus.config.js
```

:::info

此迁移不会更改 `/docs` 文件夹的位置，但 Docusaurus v2 站点通常将 `/docs` 文件夹放在 `/website` 内部。

迁移到 v2 后，您可以自由地将 `/docs` 文件夹放在任何您想要的位置。

:::

## 迁移过程 {#migration-process}

要获得一个功能齐全的 Docusaurus 2 网站，需要迁移多个部分：

- 包
- CLI 命令
- 站点配置
- Markdown 文件
- 侧边栏文件
- 页面、组件和 CSS
- 版本化文档
- i18n 支持 🚧

## 自动迁移过程 {#automated-migration-process}

[迁移 CLI](./migration-automated.mdx) 将为您处理许多迁移事宜。

然而，有些部分不易自动化，您将不得不回退到手动过程。

:::note

我们建议运行迁移 CLI，并借助手动迁移过程完成缺失的部分。

:::

## 手动迁移过程 {#manual-migration-process}

迁移的某些部分无法自动化（特别是页面），您将需要手动迁移它们。

[手动迁移指南](./migration-manual.mdx)将为您提供所有手动步骤。

## 支持 {#support}

如有任何问题，您可以在 [`#migration-v1-to-v2` Discord 频道](https://discord.gg/C3P6CxMMxY)中提问。

如果您希望我们查看您的迁移 PR，请随时标记 [@slorber](https://github.com/slorber)。

我们也有志愿者愿意[帮助您迁移您的 v1 站点](https://github.com/facebook/docusaurus/issues/1834)。

## 迁移 PR 示例 {#example-migration-prs}

您可以参考我们为 [Create React App](https://github.com/facebook/create-react-app/pull/7785) 和 [Flux](https://github.com/facebook/flux/pull/471) 的迁移 PR，作为如何为一个基本的 Docusaurus v1 站点进行迁移的示例。
```
